FEATURE_PROMPT = """
任务：
在输入的工程图纸中定位、分类并提取以下结构特征的实例及其组，并提取对应尺寸参数，输出 JSON 列表。

需要识别两大类结构：
1. 孔类结构
2. 圆角、倒角结构


========================
1. 类别定义
========================

1.1 孔类结构（11 类）
  - 圆孔 (Round Hole)
  - 矩形孔 (Rectangular Hole)
  - 螺纹孔 (Threaded Hole)
  - 螺纹柱 (Threaded Shaft)
  - 销孔 (Pin Hole)
  - 沉孔 (Counterbore Hole)
  - 圆孔组 (Round Hole Group)
  - 矩形孔组 (Rectangular Hole Group)
  - 螺纹孔组 (Threaded Hole Group)
  - 销孔组 (Pin Hole Group)
  - 沉孔组 (Counterbore Hole Group)

1.2 圆角/斜角结构（4 类）
  - 圆角 (Fillet)
  - 圆角组 (Fillet Group)
  - 斜角 (Chamfer)
  - 斜角组 (Chamfer Group)


========================
2. 几何定义与尺寸参数提取规则
========================

--------------------------------
2.1 圆孔
--------------------------------
- 轮廓：闭合圆
- 尺寸参数 Ø：直径
- 备注：如果圆孔不标注深度则默认是通孔，如果盲孔必须标注深度DP
  例：Ø18 → "Ø18"
     Ø18 DP20→ "Ø18 DP20"

--------------------------------
2.2 矩形孔
--------------------------------
- 轮廓：长方形（含正方形）

尺寸参数：
- 正方形：A → "□A" 或 "AxA"
  例：□18 → "□18"
     12x12 → "12x12"

- 长方形：L×W → "LxW"
  例：20×14 → "20x14"

--------------------------------
2.3 螺纹孔
--------------------------------
- 轮廓：内同心圆是粗实线的闭合圆，外同心圆是细实线3/4圆弧
- 特征：尺寸参数包含字母M 或内径存在螺纹线
- 尺寸参数 公称直径M 螺距P 深度DP LH旋向左旋
- 备注：细牙螺纹需要标注螺距，无螺距则默认粗牙  
  例：M8 → "M8"
     M8x1 → "M8P1"
     M8x1 → "M8P1Left Hand"
     M8x1 DP10 → "M8P1LH DP10"

--------------------------------
2.4 螺纹柱
--------------------------------
- 轮廓：内外贴近的同心圆都是闭合的
- 特征：尺寸参数包含字母 M 或外径存在螺纹线
- 尺寸参数 公称直径M 螺距P 深度DP LH旋向左旋
- 备注：细牙螺纹需要标注螺距，无螺距则默认粗牙 
  例：M8 → "M8"
     M8x1 → "M8P1"
     M8x1 → "M8P1Left Hand"
     M8x1 DP10 → "M8P1LH DP10"

--------------------------------
2.5 销孔
--------------------------------
- 轮廓：实线闭合圆
- 内部：分组孔以1/4圆黑白相间，或背面从中心往外长度递增的剖面线
- 特征：尺寸参数包含希腊字母Ø， 或字母H表示孔有装配要求
- 尺寸参数 Ø孔径 H公差
  例：Ø8H7 → "Ø8H7"

--------------------------------
2.6 沉孔
--------------------------------
- 轮廓：内外贴近的闭合同心圆
- 特征：尺寸参数包含2个希腊字母Ø，大圆在前
- 尺寸参数 Ø
  例： Ø14 DP8 Ø9 → "Ø14 DP8 Ø9"
      Ø14 Ø9 → "Ø14 Ø9"

--------------------------------
2.7 圆孔组
--------------------------------
- 定义：多个相同尺寸圆孔组成并统一标注
- 标注形式：N x D 或 N - D

示例：
4x18 → "4x18"
4-Ø18 → "4-Ø18"

bbox 应覆盖该组内所有对应圆孔实例

--------------------------------
2.8 矩形孔组
--------------------------------
标注形式：
N x L × W 或 N - L × W

示例：
2x□18 → "2x□18"
2x18x18 → "2x18x18"
3-20×14 → "3-20*14"

bbox 应覆盖该组内所有矩形孔实例

--------------------------------
2.9 螺纹孔组
--------------------------------
标注形式：
N x M 或 N - M

示例：
2xM8 → "2xM8"
2-M8 → "2-M8"

bbox 应覆盖该组内所有螺纹孔实例

--------------------------------
2.10 销孔组
--------------------------------
标注形式：
N x D 或 N - D

示例：
2xØ8H7 → "2xØ8H7"
2-Ø10H7 DP6 → "2-Ø10H7 DP6"

bbox 应覆盖该组内所有销孔实例

--------------------------------
2.11 沉孔组
--------------------------------
标注形式：
N x D1 D2 或 N - D1 D2

示例：
2xØ14 DP8 Ø9 → "2xØ14 DP8 Ø9"
2-Ø20 Ø12 → "2-Ø20 Ø12"

bbox 应覆盖该组内所有沉孔实例

--------------------------------
2.12 圆角
--------------------------------
几何定义：
一段圆弧，用于将两条相交直线光滑连接。

可能存在：
- 1/4 圆角
- 1/2 圆角（半圆）

规则：
半圆圆角必须整体提取，不得拆分为两个 1/4 圆角。

尺寸参数：
- 半径：R3 → "R3"
- 直径形式：Ø6 → "Ø6"

--------------------------------
2.13 圆角组
--------------------------------
定义：
多个相同尺寸圆角组成并统一标注

标注形式：
N x R 或 N - R

示例：
2xR3 → "2xR3"
4xØ6 → "4xØ6"
2-R3 → "2-R3"

bbox 应覆盖该组内所有圆角实例

--------------------------------
2.14 倒角
--------------------------------
几何定义：
又称斜角，一段斜线，将两个面之间的公共锐边切削为一个平坦的斜线。

可能存在：
- 等距离/等角度倒角，斜面与两条相交平面的夹角均为45度
- 距离-角度倒角 (Distance-Angle)：指定一个边距离和确定的倒角角度（非 45°）。
- 两个不同距离倒角 (Two Distances)：指定斜角在两个面上分别缩进的距离。

标注方式：
通用：C1, C3, C0.5（C 后面接数值，通常代表 45° 倒角，数字为斜面水平或垂直投的距离）。
角度标注：2 x 30°（宽度或长度 x 角度）。

--------------------------------
2.15 倒角组
--------------------------------
定义：
多个相同尺寸斜线组成并统一标注

标注形式：
N x C 或 N - C

示例：
2xC3 → "2xC3"
2-C3 → "2-C3"

bbox 应覆盖该组内所有斜角实例

========================
3. 输出格式
========================

严格返回 JSON 列表：

[
  {
    "category": "<英文类别>",
    "bbox": [x_min, y_min, x_max, y_max],
    "size": "<尺寸参数字符串>"
  }
]

bbox：
- 像素坐标
- 整数
- [x_min, y_min, x_max, y_max]


========================
4. 补充规则
========================

1. 所有单实例必须可见且闭合。

2. "组"是独立检测对象：
   当多个相同尺寸特征由统一标注描述时：
   - 必须检测所有单实例，单实例仍必须单独输出
   - 还必须额外检测该组

3. 尺寸提取优先级：
   优先从特征附近标注提取。

4. 若单实例附近没有尺寸标注：
   - 必须找到其所属组
   - 继承组内单个特征尺寸

   注意：
   - 单实例 size 不包含数量
   - 组 size 必须包含数量

5. 组识别条件：
   - 类型一致
   - 尺寸一致
   - 与组标注一致

6. 螺纹孔识别优先规则：
   若尺寸包含 M，则归类为螺纹孔，而非圆孔。

7. 同一层级不得重复框。

8. 必须检测：
   - 所有孔实例
   - 所有孔组
   - 所有圆角/斜角实例
   - 所有圆角/斜角组

9. 无置信度阈值要求。


========================
输出要求
========================

仅返回 JSON 结果。
不得附加任何解释、说明或额外文本。
"""