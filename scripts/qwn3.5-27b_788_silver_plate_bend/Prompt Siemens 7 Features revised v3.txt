FEATURE_PROMPT = """
任务：
在输入的工程图纸中定位、分类并提取以下结构特征的实例及其组，并提取对应尺寸参数，输出 JSON 列表。

需要识别三大类：
1. 孔类结构
2. 圆角结构
3. 折弯、镀银



========================
1. 类别定义
========================

1.1 孔类结构（4 类）
  - 圆孔 (Round Hole)
  - 矩形孔 (Rectangular Hole)
  - 螺纹孔 (Threaded Hole)
  - 腰孔 (Slotted Hole)


1.2 圆角结构（1 类）
  - 圆角 (Fillet)


1.3 工艺特征（2 类）
  - 折弯 (Bending)
  - 镀银 (Silver Plating)


========================
2. 几何定义与尺寸参数提取规则
========================

--------------------------------
2.1 圆孔
--------------------------------
- 轮廓：闭合圆
- 尺寸参数 Ø：直径
- 备注：如果圆孔不标注深度则默认是通孔，如果盲孔必须标注深度DP
  例：Ø18 → "Ø18"
     Ø18 DP20→ "Ø18 DP20"

--------------------------------
2.2 矩形孔
--------------------------------
- 轮廓：长方形（含正方形）

尺寸参数：
- 正方形：A → "□A" 或 "AxA"
  例：□18 → "□18"
     12x12 → "12x12"

- 长方形：L×W → "LxW"
  例：20×14 → "20x14"

--------------------------------
2.3 螺纹孔
--------------------------------
- 轮廓：内同心圆是粗实线的闭合圆，外同心圆是细实线3/4圆弧
- 特征：尺寸参数包含字母M 或内径存在螺纹线
- 尺寸参数： 公称直径M 螺距P 深度DP LH旋向左旋
- 备注：细牙螺纹需要标注螺距，无螺距则默认粗牙  
  例：M8 → "M8"
     M8x1 → "M8P1"
     M8x1 → "M8P1Left Hand"
     M8x1 DP10 → "M8P1LH DP10"

--------------------------------
2.4 腰孔
--------------------------------
- 轮廓：两端为直径相等的半圆弧，中间为两条与半圆弧相切的平行直线段的封闭异形孔
- 特征：长圆形孔洞结构，确定腰孔的形状和大小
- 尺寸参数： 半圆弧直径Ø， 半圆弧半径R，及平行直线段的长度L，或矩形宽度W
- 备注：三种不同的标注方式
  例：Ø10 20 → "Ø10 20"
     2xR5 20 → "2xR5 20"
     10x20 → "10x20"

--------------------------------
2.5 圆角
--------------------------------
几何定义：
一段圆弧，用于将两条相交直线光滑连接。

可能存在：
- 1/4 圆角
- 1/2 圆角（半圆）

规则：
半圆圆角必须整体提取，不得拆分为两个 1/4 圆角。

尺寸参数：
- 半径：R3 → "R3"
- 直径形式：Ø6 → "Ø6"

--------------------------------
2.6 折弯
--------------------------------
定义：
具有一定曲率形状的几何截面，相邻板材或管材形成V形角度

标注形式：
90° R3 或 45° Rmin

示例：
90° R3 → "90° R3"
45° Rmin → "45° Rmin"
90° → "90°"

bbox 应覆盖折弯及相邻的两段板材或管材

--------------------------------
2.7 镀银
--------------------------------
定义：
用虚线框标出来的一段板材，表示在金属或非金属基体表面覆盖一层薄薄的银层

标注形式：
L 长度 或 L 长度加公差范围 或contact surface接触表面 

示例：
105 +5/0 → "105 +5/0"
90  → "90"

bbox 应覆盖折弯及相邻的两段板材或管材

========================
3. 输出格式
========================

严格返回 JSON 列表：

[
  {
    "category": "<英文类别>",
    "bbox": [x_min, y_min, x_max, y_max],
    "size": "<尺寸参数字符串>"
  }
]

bbox：
- 像素坐标
- 整数
- [x_min, y_min, x_max, y_max]


========================
4. 补充规则
========================

1. 所有单实例必须可见且闭合。

2. "组"是独立检测对象：
   当多个相同尺寸特征由统一标注描述时：
   - 必须检测所有单实例，单实例仍必须单独输出
   - 还必须额外检测该组

3. 尺寸提取优先级：
   优先从特征附近标注提取。

4. 若单实例附近没有尺寸标注：
   - 必须找到其所属组
   - 继承组内单个特征尺寸

   注意：
   - 单实例 size 不包含数量
   - 组 size 必须包含数量

5. 组识别条件：
   - 类型一致
   - 尺寸一致
   - 与组标注一致

6. 螺纹孔识别优先规则：
   若尺寸包含 M，则归类为螺纹孔，而非圆孔。

7. 同一层级不得重复框。

8. 必须检测：
   - 所有孔实例
   - 所有圆角实例
   - 所有工艺特征实例

9. 无置信度阈值要求。


========================
输出要求
========================

仅返回 JSON 结果。
不得附加任何解释、说明或额外文本。
"""